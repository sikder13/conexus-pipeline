"""Source adapters: one per public grant-recipient dataset we extract from.

Each adapter corresponds to a row in the `source_adapters` table from
migration 001. Adding a region means adding an adapter here and a row there,
not editing the extractor.
"""
