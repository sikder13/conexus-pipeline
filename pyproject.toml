[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "conexus-pipeline"
version = "0.1.0"
description = "Prospect research and outreach pipeline for Nahl Technologies"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "pydantic>=2.7,<3",
    "rich>=13.7",
    "anthropic>=0.40",
    "supabase>=2.15",
    "python-dotenv>=1.0",
    # Console groundwork. The review console is not built yet; these are
    # declared now so the environment is ready and versions are pinned once.
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "jinja2>=3.1",
    # PDF dossiers. Pure Python, no system libraries and no network at render
    # time, so a report generates the same on a laptop with the cable out.
    "reportlab>=4.2",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.6",
    "pytest>=8.0",
    # Reads generated PDFs back so the tests assert on rendered text rather
    # than on the code that was supposed to render it.
    "pdfplumber>=0.11",
]

[tool.setuptools]
packages = [
    "lib",
    "tools",
    "tools.drafter",
    "tools.extractor",
    "tools.harvester",
    "tools.logger",
    "tools.verifier",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]

[tool.ruff.lint.isort]
known-first-party = ["lib", "tools"]
# The repo-root `supabase/` directory holds migrations, not a Python package;
# without this, isort mistakes the supabase-py import for first-party code.
known-third-party = ["supabase"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
